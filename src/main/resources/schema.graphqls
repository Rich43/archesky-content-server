type Query {
    listContent: [Content]!
}

type Mutation {
    createContent(content: String!, token: String!): Content!
    updateContent(id: ID!, content: String!, token: String!): Content
    deleteContent(id: ID!, token: String!): Content
}

type Content {
    id: ID!
    content: String!
}
